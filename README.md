# City Events Tracker

CSE412 Final Project

## Features

- `/` View all events
- `/feed` Personalized feed
- `/admin` Add / update / delete events

## Tech Stack

- Next.js
- PostgreSQL
- Tailwind CSS

## Run Locally

```bash
npm install
npm run dev

Then open:

http://localhost:3000

Load the PostgreSQL schema/data using the provided SQL scripts.

Team Notes

Use GitHub to pull latest changes.


Then:

```bash
git add README.md
git commit -m "Update README"
git push
